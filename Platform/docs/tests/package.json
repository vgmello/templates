{
  "name": "platform-docs-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Platform Operations Service documentation",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "serve": "python3 -m http.server 8080 --directory ../_site",
    "serve:alt": "npx http-server ../_site -p 8080 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}