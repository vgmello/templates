{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:serve": "vitepress serve",
    "sync": "cp -r content/*.md . && echo 'Documentation files synced from DocFX content'",
    "build": "pnpm sync && pnpm docs:build",
    "dev": "pnpm sync && pnpm docs:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}